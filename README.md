🌤️ AI Weather Forecast Project
📋 Описание проекта
Проект представляет собой интеллектуальный генератор прогнозов погоды, который использует искусственный интеллект для создания красивых, атмосферных прогнозов на основе данных из Яндекс.Погоды. Вместо сухих метеоданных пользователь получает творчески оформленный прогноз, подчеркивающий, что он создан ИИ.

✨ Основные особенности
🤖 Генерация прогнозов с помощью ИИ — преобразование сырых данных в живые, атмосферные тексты

🗺️ Геокодирование — поиск городов России по названию с автоматическим определением координат

🎨 Чистый визуальный язык — прогнозы содержат только эмодзи, цифры и пробелы

📱 Modern React + Next.js — современный стек технологий

🔄 React Server Actions — для обработки форм на сервере

🏗️ Архитектура проекта
text
┌─────────────────────────────────────────────┐
│   Клиентский компонент (React)              │
│   • WeatherWidget                           │
│   • Ввод города с автодополнением           │
│   • Отображение результата                  │
└───────────────┬─────────────────────────────┘
                │
┌───────────────▼─────────────────────────────┐
│   Серверный API Route (Next.js)             │
│   • POST /api/weather                       │
│   • Получает координаты → возвращает прогноз│
└───────────────┬─────────────────────────────┘
                │
         ┌──────┴──────┐
         ▼             ▼
┌──────────────┐ ┌────────────────────┐
│ Геокодирование│ │ Яндекс Погода API │
│   (Open-Meteo)│ │ (текущая погода)   │
└──────────────┘ └────────────────────┘
         │                   │
         └─────────┬─────────┘
                   ▼
          ┌─────────────────┐
          │   OpenRouter AI │
          │   (DeepSeek)    │
          └─────────────────┘
🛠️ Технологический стек
Фронтенд:
Next.js 14 (App Router)

React 19 (с Server Actions)

TypeScript — типизация

Tailwind CSS — стилизация

API и сервисы:
OpenRouter API — доступ к моделям ИИ (DeepSeek)

Яндекс.Погода API — получение метеоданных

Open-Meteo Geocoding API — геокодирование (город → координаты)

Инфраструктура:
Environment Variables — хранение API-ключей

React Server Components/Server Actions — серверная логика

Error Boundaries — обработка ошибок

📁 Структура проекта
text
weather-ai-app/
├── app/
│   ├── api/
│   │   └── weather/
│   │       └── route.ts          # API endpoint для прогноза
│   ├── weatherAction.ts          # Server Action для формы
│   └── page.tsx                  # Главная страница
├── components/
│   └── WeatherWidget.tsx         # Виджет погоды
├── public/
│   └── ...                       # Статические файлы
├── .env.local                    # Переменные окружения
├── package.json
├── tsconfig.json
└── tailwind.config.js
🔑 Настройка API-ключей
Для работы проекта необходимы следующие API-ключи (добавить в .env.local):

env
# OpenRouter для доступа к моделям ИИ
OPENROUTER_API_KEY=your_openrouter_key_here

# Яндекс.Погода для получения метеоданных
YANDEX_WEATHER_KEY=your_yandex_weather_key_here

# (Опционально) DaData для более точного геокодирования по России
DADATA_API_KEY=your_dadata_key_here
DADATA_SECRET_KEY=your_dadata_secret_here
Где получить ключи:
OpenRouter — openrouter.ai

Яндекс.Погода — developer.tech.yandex.ru

DaData — dadata.ru (опционально, для точного геокодирования)

🚀 Установка и запуск
1. Клонирование и установка зависимостей
bash
git clone <repository-url>
cd weather-ai-app
npm install
2. Настройка переменных окружения
bash
cp .env.example .env.local
# Отредактируйте .env.local, добавив ваши API-ключи
3. Запуск в режиме разработки
bash
npm run dev
Приложение будет доступно по адресу: http://localhost:3000

4. Сборка для продакшена
bash
npm run build
npm start
🎯 Использование
Ввод города — начните вводить название города России в поле ввода

Выбор из подсказок — выберите город из выпадающего списка

Генерация прогноза — нажмите "Получить прогноз"

Просмотр результата — наслаждайтесь красивым прогнозом от ИИ
