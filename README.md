
# 🌤️ AI Weather

## 📋 Описание проекта
Проект представляет собой интеллектуальный генератор прогнозов погоды, который использует искусственный интеллект для создания красивых, атмосферных прогнозов на основе данных из OPENWEATHER. Вместо сухих метеоданных пользователь получает творчески оформленный прогноз, подчеркивающий, что он создан ИИ.

## ✨ Основные особенности
- **🤖 Генерация прогнозов с помощью ИИ** — преобразование сырых данных в живые, атмосферные тексты.
- **🗺️ Геокодирование** — поиск городов по названию с автоматическим определением координат.
- **🎨 Чистый визуальный язык** — прогнозы содержат только эмодзи, цифры и пробелы.
- **📱 Modern React + Next.js** — современный стек технологий.
- **🔄 React Server Actions** — для обработки форм на сервере.

## 🏗️ Архитектура проекта
```plaintext
┌─────────────────────────────────────────────┐
│ Клиентский компонент (React)               │
│ • WeatherWidget                            │
│ • Ввод города с автодополнением            │
│ • Отображение результата                   │
└───────────────┬─────────────────────────────┘
                │
                ▼
┌───────────────▼─────────────────────────────┐
│ Серверный API Route (Next.js)               │
│ • POST /api/weather                         │
│ • Получает координаты → возвращает прогноз │
└───────────────┬─────────────────────────────┘
                │
                ▼
         ┌──────┴──────┐ ▼ ▼ ┌──────────────┐  ┌────────────────────┐
         │ Геокодирование│ │ OPENWEATHER API │ │ (Open-Meteo)│ │ (текущая погода) │
         └──────────────┘ └──────────────────┘ └────────────────────┘
                           │
                           └─────────┬─────────┘
                                       ▼
                              ┌─────────────────┐
                              │ OpenRouter AI   │
                              │ (DeepSeek)      │
                              └─────────────────┘
```

## 🛠️ Технологический стек
### Фронтенд:
- **Next.js 16 (App Router)** — для построения React-приложения с серверной логикой.
- **React 19** (с Server Actions) — для динамической работы с компонентами.
- **TypeScript** — типизация для повышения стабильности и производительности.
- **Tailwind CSS** — для быстрой и гибкой стилизации.

### API и сервисы:
- **OpenRouter API** — доступ к моделям ИИ (DeepSeek) для генерации прогнозов.
- **OPENWEATHER API** — получение метеоданных о текущей погоде.
- **Open-Meteo Geocoding API** — геокодирование для преобразования названия города в координаты.

### Инфраструктура:
- **Environment Variables** — хранение API-ключей.
- **React Server Components/Server Actions** — для обработки логики на сервере.
- **Error Boundaries** — обработка ошибок в приложении.

## 📁 Структура проекта
```plaintext
weather-ai-app/
├── app/
│   ├── api/
│   │   └── weather/
│   │       └── route.ts       # API endpoint для прогноза
│   ├── weatherAction.ts       # Server Action для формы
│   └── page.tsx               # Главная страница
├── components/
│   └── WeatherWidget.tsx      # Виджет погоды
├── public/
│   └── ...                    # Статические файлы
├── .env                       # Переменные окружения
├── package.json
├── tsconfig.json
└── tailwind.config.js
```

## 🔑 Настройка API-ключей
Для работы проекта необходимы следующие API-ключи. Убедитесь, что они добавлены в файл `.env`:

```plaintext
# OpenRouter для доступа к моделям ИИ
OPENROUTER_API_KEY=your_openrouter_key_here

# Яндекс.Погода для получения метеоданных
OPENWEATHER_API_KEY=your_yandex_weather_key_here
```

### Где получить ключи:
- **OpenRouter** — [openrouter.ai](https://openrouter.ai)
- **OPENWEATHER** — [https://openweathermap.org](https://openweathermap.org)
- **DaData** (опционально) — [dadata.ru](https://dadata.ru)

## 🚀 Установка и запуск

1. **Клонирование репозитория и установка зависимостей:**
   ```bash
   git clone https://github.com/yourusername/weather-ai-app.git
   cd weather-ai-app
   npm install
   ```

2. **Настройка переменных окружения:**
   Создайте файл `.env` и отредактируйте его, добавив свои API-ключи.

3. **Запуск в режиме разработки:**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу: [http://localhost:3000](http://localhost:3000).

4. **Сборка для продакшена:**
   ```bash
   npm run build
   npm start
   ```

## 🎯 Использование

1. **Ввод города** — начните вводить название города в поле ввода.
2. **Выбор из подсказок** — выберите город из выпадающего списка.
3. **Генерация прогноза** — нажмите "Получить прогноз".
4. **Просмотр результата** — наслаждайтесь красивым прогнозом от ИИ, который будет сгенерирован и отображён в визуальном формате.

